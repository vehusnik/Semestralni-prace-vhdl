<Window x:Class="Semestralni_prace_vhdl.Views.LekarskaZpravaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Semestralni_prace_vhdl.Views"
        mc:Ignorable="d"
        Title="Lékařská zpráva" Height="500" Width="600"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Datum vyšetření:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
            <DatePicker SelectedDate="{Binding DatumVysetreni}" Width="150" HorizontalAlignment="Left"/>
        </StackPanel>

        <TextBlock Grid.Row="1" Text="Symptomy (co pacienta trápí):" FontWeight="Bold"/>
        <TextBox Grid.Row="2" Text="{Binding Symptomy}" 
                 Margin="0,5,0,15" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

        <TextBlock Grid.Row="3" Text="Diagnóza (co lékař zjistil):" FontWeight="Bold"/>
        <TextBox Grid.Row="4" Text="{Binding Diagnoza}" 
                 Margin="0,5,0,15" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

        <TextBlock Grid.Row="5" Text="Doporučení a léčba:" FontWeight="Bold"/>
        <TextBox Grid.Row="6" Text="{Binding Doporuceni}" 
                 Margin="0,5,0,15" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

        <Button Grid.Row="7" Content="Uložit zprávu do databáze" 
                Command="{Binding UlozitCommand}"
                Height="40" FontSize="14" FontWeight="Bold" Background="#DDDDDD"/>
    </Grid>
</Window>